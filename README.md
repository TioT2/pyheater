# Общая идея/цель проекта
Симуляция теплопередачи в **твёрдом** теле.

# Команда
Чудаков ТК (Б05-503).

# Библиотеки
Пока не решено, что-нибудь для работы с GPU (OpenGL/Vulkan/WebGPU/???) (ибо перестроение сетки и обновление симуляции на CPU работают уж слишком медленно + хочется интерактивности).

# Фичи
SDF-based CSG для построения объектов, построение изоповерхностей по воксельной сетке, UI (как вариант).
