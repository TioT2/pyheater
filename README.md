# Общая идея/цель проекта
(Интерактивная) симуляция теплопередачи в **твёрдом** теле.

# Команда
Чудаков Т.К. (Б05-503).

# Сторонние библиотеки
Пока не решено, что-нибудь для расчётов на GPU (OpenGL/Vulkan/WebGPU/???, перестроение сетки это и обновление симуляции это высокопараллельные задачи - на CPU работают уж слишком медленно + хочется так или иначе достичь интерактивности для не слишком больших разрешений) и для рендеринга (pyvista + pyvistaqt, как вариант, либо кастомный рендер (для интеграции с вычислениями) с родственником ImGUI поверх него).

# Фичи
Здесь (намечен) список фич для 1 и 2 итераций.

## Итерация 1
SDF-based CSG для построения объектов, изоповерхности, симуляция. Экспорт результатов в видео.

## Итерация 2
GUI, возможность интерактивной настройки и влияния на симуляцию.
